Schema API
==========

For any use of the Python API, you will probably need to create a Schema object.

The schema object provides information about the schema to the other functions.

When there are different versions of the schema, different schema objects will be available to choose from.

For now, use:


.. autoclass:: libcoveofds.schema.OFDSSchema
  :members:
